zerops:
  - setup: admin
    build:
      base: nodejs@18
      buildCommands:
        - yarn
        - |
          export NODE_ENV=production
          yarn build:admin
      cache:
        - ./node_modules
        - ./yarn.lock
      deploy:
        - ./build/~
